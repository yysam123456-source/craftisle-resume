# Server (not needed for local-storage-only mode)
apps/server

# Database package (server-only, needs postgres driver)
packages/db

# Auth package (server-only, better-auth)
packages/auth

# Root level
packages
apps

# Only keep apps/web
!apps/web

# Build caches
node_modules
*.log
.vite
.dist
coverage
